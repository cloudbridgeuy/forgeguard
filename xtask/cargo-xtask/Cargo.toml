[package]
name = "cargo-xtask"
version = "0.1.0"
publish = false
license = "MIT"
edition = "2021"
description = "Thin wrapper that skips cargo's fingerprint when target/debug/xtask is fresh — installed via cargo install"

[[bin]]
name = "cargo-xtask"
path = "src/main.rs"

[dependencies]
