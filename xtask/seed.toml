# Seed configuration for `cargo xtask control-plane seed`.
# All seeded orgs are created as Draft. No users are created here —
# user provisioning has moved to issue #100 (per-org Cognito pools
# land via the saga ticket, separate from #102).

[[organization]]
org_id = "org-acme"
name = "Acme Corp"

[[organization.group]]
name = "member"
description = "Read-only org access"
allow = ["cp-organization-read", "cp-key-read", "cp-config-read"]

[[organization.group]]
name = "admin"
description = "Org management without deletion or owner promotion"
inherits = ["member"]
allow = [
  "cp-organization-create", "cp-organization-update",
  "cp-member-invite", "cp-member-remove", "cp-member-change-role",
  "cp-config-write",
  "cp-key-generate", "cp-key-revoke", "cp-key-rotate",
]

[[organization.group]]
name = "owner"
description = "Full org access including deletion and owner promotion"
inherits = ["admin"]
allow = ["cp-organization-delete", "cp-member-promote-owner"]

[[organization]]
org_id = "org-globex"
name = "Globex Corporation"

[[organization.group]]
name = "member"
description = "Read-only org access"
allow = ["cp-organization-read", "cp-key-read", "cp-config-read"]

[[organization.group]]
name = "admin"
description = "Org management without deletion or owner promotion"
inherits = ["member"]
allow = [
  "cp-organization-create", "cp-organization-update",
  "cp-member-invite", "cp-member-remove", "cp-member-change-role",
  "cp-config-write",
  "cp-key-generate", "cp-key-revoke", "cp-key-rotate",
]

[[organization.group]]
name = "owner"
description = "Full org access including deletion and owner promotion"
inherits = ["admin"]
allow = ["cp-organization-delete", "cp-member-promote-owner"]
