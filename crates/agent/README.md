# forgegate_agent

ForgeGate self-hosted data plane agent. This is an **I/O binary crate**.

Deployed in customer AWS accounts. Handles authorization decisions locally and syncs configuration from the control plane.
