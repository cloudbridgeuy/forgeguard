# forgegate_control_plane

ForgeGate control plane API server. This is an **I/O binary crate**.

Owns the dashboard backend (Model Studio, Operations, Webhooks, God Mode), tenant management, user management, and webhook delivery engine. Built with axum.
