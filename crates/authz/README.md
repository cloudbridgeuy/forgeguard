# forgeguard_authz

Authorization I/O layer for ForgeGuard. This is an **I/O crate** — it depends on pure crates and adds side effects.

Owns the Verified Permissions client, Cedar policy sync, ElastiCache decision caching, and permission evaluation.
